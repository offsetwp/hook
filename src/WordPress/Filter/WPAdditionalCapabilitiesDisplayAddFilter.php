<?php
/**
 * WPAdditionalCapabilitiesDisplayAddFilter
 *
 * @package Offsetwp\Hook\WordPress\Filter
 */

declare( strict_types=1 );

namespace Offsetwp\Hook\WordPress\Filter;

/**
 * Filters whether to display additional capabilities for the user.
 *
 * @since 2.8.0
 */
abstract class WPAdditionalCapabilitiesDisplayAddFilter extends \Offsetwp\Hook\Support\AddFilter {
	/**
	 * The name of the action to add the callback to.
	 *
	 * @var string
	 */
	public string $hook_name = 'additional_capabilities_display';

	/**
	 * The callback to be run when the action is called.
	 *
	 * @var string
	 */
	public int $hook_priority = 10;

	/**
	 * Optional. Used to specify the order in which the functions associated with a particular action are executed. Lower numbers correspond with earlier execution, and functions with the same priority are executed in the order in which they were added to the action. Default 10.
	 *
	 * @var string
	 */
	public int $hook_accepted_args = 2;


	/**
	 * The hook execution method
	 *
	 * @param bool $enable Whether to display the capabilities. Default true.
	 * @param \WP_User $profile_user The current WP_User object.
	 */
	abstract public function execute( $enable, $profile_user );
}
