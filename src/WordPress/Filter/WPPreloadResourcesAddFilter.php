<?php
/**
 * WPPreloadResourcesAddFilter
 *
 * @package Offsetwp\Hook\WordPress\Filter
 */

declare( strict_types=1 );

namespace Offsetwp\Hook\WordPress\Filter;

/**
 * Filters domains and URLs for resource preloads.
 *
 * @since 6.1.0
 * @since 6.6.0 Added the `$fetchpriority` attribute.
 */
abstract class WPPreloadResourcesAddFilter extends \Offsetwp\Hook\Support\AddFilter {
	/**
	 * The name of the action to add the callback to.
	 *
	 * @var string
	 */
	public string $hook_name = 'wp_preload_resources';

	/**
	 * The callback to be run when the action is called.
	 *
	 * @var string
	 */
	public int $hook_priority = 10;

	/**
	 * Optional. Used to specify the order in which the functions associated with a particular action are executed. Lower numbers correspond with earlier execution, and functions with the same priority are executed in the order in which they were added to the action. Default 10.
	 *
	 * @var string
	 */
	public int $hook_accepted_args = 1;


	/**
	 * The hook execution method
	 *
	 * @param array $preload_resources Array of resources and their attributes, or URLs to print for resource preloads.
	 */
	abstract public function execute( $preload_resources );
}
